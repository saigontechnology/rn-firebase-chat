# Required for React Native / Metro bundler compatibility
# Metro does not follow pnpm symlinks; hoisted layout puts all packages
# at a single depth that Metro can resolve without custom logic.
node-linker=hoisted
ignore-scripts=true
allow-git=none
minimum-release-age=7d