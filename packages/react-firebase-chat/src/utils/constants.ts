export {
  DEFAULT_ITERATIONS,
  DEFAULT_KEY_LENGTH,
  DEFAULT_CLEAR_SEND_NOTIFICATION,
  DEFAULT_TYPING_TIMEOUT_MS as DEFAULT_TYPING_TIMEOUT_SECONDS,
} from '@saigontechnology/firebase-chat-shared';

// Web-specific constant not in shared
export const MAX_INPUT_LENGTH: number = 1000;
